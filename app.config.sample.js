//
//
// Application configuration

const config = {
	appConfig: {
		'version' : '0.1.0'
	},
	dbConfig:{
		host: 'dbserver.com',
		port: '3306',
		user: 'me',
		password: 'pw',
		database: 'dbName'
	}
}

module.exports = config;
